<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cafe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- light mode -->
    <!-- screen background -->
    <style name="SCREEN">
        <item name="android:background">#fff</item>
    </style>
    <!--Text background-->
    <style name="TEXT">
        <item name="android:textColor">#000</item>
    </style>

    <style name="Theme.Cafe" parent="Base.Theme.Cafe" />
    <style name="Theme.Cafe.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">#FF4800</item>
    </style>
    <style name="Theme.Cafeteria.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Cafeteria.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>